from collections import namedtuple

x = namedtuple('C', 'g b c') # Именованый кортеж

y = [x('N', 1, 8), x('Br', 8, 14)] # Создание обьекта по namedtupl'у

print(y[0])

print(y[0].g) # Вывод элемента

# Именованые кортежи можно описать как классы без функций, грубо говоря:
# Контейнер неизменяемых данных
